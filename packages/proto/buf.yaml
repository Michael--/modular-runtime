version: v2
# name: buf.build/michael--/modular-runtime

modules:
  - path: .
    excludes:
      - node_modules

lint:
  use:
    - STANDARD
  except:
    - ENUM_VALUE_PREFIX
    - ENUM_ZERO_VALUE_SUFFIX

breaking:
  use:
    - FILE
