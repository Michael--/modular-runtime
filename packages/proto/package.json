{
  "name": "@modular-runtime/proto",
  "private": true,
  "type": "module",
  "scripts": {
    "clean": "rimraf dist generated",
    "lint": "buf lint",
    "gen": "PATH=$PWD/node_modules/.bin:$PATH:$HOME/go/bin buf generate",
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "breaking": "buf breaking --against '.git#branch=main'"
  },
  "exports": {
    "./*": "./dist/*"
  },
  "devDependencies": {
    "@bufbuild/protobuf": "^2.11.0",
    "@grpc/grpc-js": "^1.14.3",
    "@types/node": "^25.1.0",
    "rimraf": "^6.1.2",
    "ts-proto": "^2.11.1"
  }
}
