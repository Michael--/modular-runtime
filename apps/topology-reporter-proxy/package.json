{
  "name": "@modular-runtime/topology-reporter-proxy",
  "private": true,
  "type": "module",
  "scripts": {
    "clean": "rimraf dist",
    "build": "vite build",
    "dev": "vite build --watch",
    "start": "node dist/topology-reporter-proxy.js",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@grpc/grpc-js": "^1.10.9",
    "@modular-runtime/topology-reporter": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^25.1.0",
    "rimraf": "^6.1.2",
    "typescript": "^5.5.4",
    "vite": "^7.3.1"
  }
}
