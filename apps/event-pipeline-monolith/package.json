{
  "name": "@modular-runtime/event-pipeline-monolith",
  "private": true,
  "type": "module",
  "scripts": {
    "clean": "rm -rf build",
    "build": "cmake -S . -B build -DCMAKE_BUILD_TYPE=Release && cmake --build build",
    "start": "./build/event-pipeline-monolith"
  }
}
